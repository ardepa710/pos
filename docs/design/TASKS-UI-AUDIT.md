# UI Audit — Tracked Issues · 2026-05-08

Companion to [UI_AUDIT.md](./UI_AUDIT.md). Each row is a discrete, actionable task. Severity follows the audit (`🔴` Critical · `🟡` Moderate · `🟢` Minor). Effort is a relative T-shirt estimate.

> **Suggested batching for PRs:** start with the `DS-*` design-system foundation tasks (they unblock the auth migration), then the auth migration, then per-area cleanups. Each batch should fit a single small PR.

---

## Stage 0 — Foundations (do first)

These unlock everything else; they're cheap and high-leverage.

| ID   | Sev | Effort | Title                                                          | File(s)                                                                                                                                                                                                         | Acceptance                                                                                                                                                                                                            |
| ---- | --- | ------ | -------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| DS-1 | 🔴  | M      | Build `<Input>` primitive                                      | new `frontend/src/components/ui/Input.tsx`; consumers in `ProductForm.tsx` 71–76, `CategoryList.tsx` 14–19, `StockAdjustModal.tsx` 22–27, `(auth)/setup/page.tsx` 46–51, `BusinessSettingsForm.tsx` 144–151     | One component with `error?: boolean`, `iconLeft?: ReactNode`, `iconRight?: ReactNode`, optional `prefix`. All 5 sites use it. Focus style is `focus:border-[var(--border-focus)]` (drop the `focus:ring` divergence). |
| DS-2 | 🔴  | M      | Build `<Button>` primitive                                     | new `frontend/src/components/ui/Button.tsx`; replace ad-hoc buttons in `PageHeader.tsx`, `ProductList.tsx` 264, `CategoryList.tsx` 304, `Cart.tsx` 56, `PaymentPanel.tsx` 539, `OpenSessionModal.tsx` 116, etc. | Variants: `primary`, `secondary`, `ghost`, `danger`. Sizes: `sm`, `md`. Built-in `isLoading`, `disabled`, `leadingIcon`, `trailingIcon`. All hand-rolled buttons across the app collapse to `<Button>`.               |
| DS-3 | 🔴  | S      | Build `<Tabs>` primitive                                       | new `frontend/src/components/ui/Tabs.tsx`; replace `CatalogManager.tsx` 24–52, `SettingsManager.tsx` 38–54                                                                                                      | Single component with `items: { key, label, icon? }[]`, `activeKey`, `onChange`, `variant: "pill" \| "segmented"`. Both call sites switch to it.                                                                      |
| DS-4 | 🔴  | S      | Resolve the two-token-systems split                            | `frontend/src/lib/design-tokens.ts`, `frontend/src/app/globals.css`                                                                                                                                             | Either delete `design-tokens.ts` (preferred — `globals.css` is the live source), **or** rewrite `globals.css` to be generated from it. After the change, grepping for `--color-bg` returns zero hits.                 |
| DS-5 | 🟡  | XS     | Promote `BRAND_DEFAULT_COLOR` constant                         | `frontend/src/lib/design-tokens.ts`, `BusinessSettingsForm.tsx` 63, 76, 102, 183, 192                                                                                                                           | Single export referenced everywhere. No literal `"#6B7A3F"` outside the constant declaration.                                                                                                                         |
| DS-6 | 🟡  | XS     | Add `hexToRgba` helper for `--accent-subtle` runtime injection | new `frontend/src/lib/color.ts`; consumers in `(app)/layout.tsx` 55, `BusinessSettingsForm.tsx` 97                                                                                                              | Helper handles 3-, 6-, and 8-digit hex; named colors throw a typed error. Both call sites updated. Unit test covers `#fff`, `#FFFFFF`, `#FFFFFF80`.                                                                   |
| DS-7 | 🔴  | XS     | Replace hardcoded red in Sidebar logout hover                  | `Sidebar.tsx` 218–222                                                                                                                                                                                           | `rgba(239,68,68,0.15)` → `var(--error-subtle)`; `#ef4444` → `var(--error)`. Visually identical in default theme; respects user's primary-color override.                                                              |
| DS-8 | 🟡  | XS     | Replace hardcoded colors in demo banner                        | `globals.css` 142–153                                                                                                                                                                                           | `background: var(--warning); color: var(--text-primary)`.                                                                                                                                                             |

---

## Stage 1 — Auth migration (depends on DS-1, DS-2)

| ID     | Sev | Effort | Title                                            | File(s)                                  | Acceptance                                                                                                                                                                                                       |
| ------ | --- | ------ | ------------------------------------------------ | ---------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| AUTH-1 | 🔴  | M      | Migrate `login` page off inline styles           | `(auth)/login/page.tsx` 92–270           | All `style={{...}}` props replaced with Tailwind classes + `<Input>` + `<Button>`. The `useEffect` that clears `--accent` etc. (lines 55–62) is **kept** — it's defensive against the runtime branding override. |
| AUTH-2 | 🔴  | M      | Migrate `change-password` page off inline styles | `(auth)/change-password/page.tsx` 45–289 | Same as AUTH-1. The `PasswordInput` sub-component becomes a thin wrapper around `<Input type="password">` with show/hide toggle.                                                                                 |
| AUTH-3 | 🔴  | XS     | Migrate `(auth)/layout.tsx` to Tailwind          | `(auth)/layout.tsx` 7–18                 | `<div class="min-h-screen flex items-center justify-center bg-[var(--bg-base)]">`.                                                                                                                               |
| AUTH-4 | 🔴  | M      | Migrate `Sidebar` off inline styles              | `Sidebar.tsx` 52–235                     | All `style={{...}}` replaced with Tailwind utility classes. Bonus: hover/active/focus states become CSS-driven (no `onMouseEnter`).                                                                              |
| AUTH-5 | 🟡  | S      | Migrate `AppShell` off inline styles             | `AppShell.tsx` 16–157                    | Same as AUTH-4 plus replace the inline `<style>{}` block (lines 148–157) with Tailwind responsive utilities (`md:flex`/`md:hidden`).                                                                             |

---

## Stage 2 — Cashier / Punto de Venta

| ID  | Sev | Effort | Title                                                                                     | File(s)                                                              | Acceptance                                                                                                                                                                                                                        |
| --- | --- | ------ | ----------------------------------------------------------------------------------------- | -------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --- | ----------------------------------- |
| C1  | 🟡  | XS     | Replace disabled "Cobrar $0.00" button when no payments added                             | `PaymentPanel.tsx` 537–559                                           | When `payments.length === 0`, render `<p class="text-sm text-[var(--text-muted)] text-center">Agrega un método de pago para cobrar.</p>` instead of the disabled button.                                                          |
| C2  | 🟢  | XS     | Replace emoji icon `⬛` on "Cerrar caja" with lucide                                      | `POSTerminal.tsx` 205                                                | `<XCircle size={14} />` or similar. Same fix for `⚠` in `ProductList.tsx` 161 and `✓` in `AppearanceSettings.tsx` 105.                                                                                                            |
| C3  | 🟢  | XS     | Replace inline `width: "55%"`/`"45%"` with Tailwind                                       | `POSTerminal.tsx` 195, 216                                           | Use `basis-[55%]` / `basis-[45%]` or `grid grid-cols-[55%_45%]`.                                                                                                                                                                  |
| C4  | 🔴  | L      | Add keyboard shortcuts to cashier flow                                                    | `POSTerminal.tsx`, `PaymentPanel.tsx`, `Cart.tsx`, `ProductGrid.tsx` | `Enter` on amount input → Agregar pago. `Ctrl+Enter` on payment list → Cobrar. `F1`–`F7` map to method buttons. `Esc` clears amount input. `/` focuses product search. Document on a `?` overlay (new component `KeyboardHints`). |
| C5  | 🟡  | XS     | Hide stock badge on healthy products in product grid                                      | `ProductGrid.tsx` 165–182                                            | Only render the chip when `outOfStock                                                                                                                                                                                             |     | lowStock`. Healthy stock = no chip. |
| C6  | 🟢  | XS     | Stabilise badge layout when both stock + consignment chips appear                         | `ProductGrid.tsx` 165–189                                            | Place both badges in a single absolute-positioned strip at the top of the card with `gap-1 flex` so the card content height is constant.                                                                                          |
| C7  | 🟡  | XS     | Bump empty-cart icon contrast                                                             | `Cart.tsx` 70–75                                                     | `text-[var(--border)]` → `text-[var(--text-muted)]`.                                                                                                                                                                              |
| C8  | 🟢  | XS     | Add `aria-label` to "Exacto" shortcut and consider rename                                 | `PaymentPanel.tsx` 410–422                                           | Add `aria-label="Llenar con el monto restante"`. Optional: rename visible text to "Restante".                                                                                                                                     |
| C9  | 🟢  | S      | Collapse always-on discount input on cart rows                                            | `CartItem.tsx` 110–148                                               | Show discount input only when (a) `hasDiscount > 0`, or (b) user clicks a small "+ Descuento" pill. Pill visible only when `canEditPrice`.                                                                                        |
| C10 | 🟡  | XS     | Clamp quantity input to ≥ 1                                                               | `CartItem.tsx` 71–84                                                 | `onChange` ⇒ `Math.max(1, parseInt(...))`. Typing `0` either clamps to 1 or triggers `onRemove(item.product_id)` with an undo banner.                                                                                             |
| C11 | 🟢  | S      | Make pending-payment amounts inline-editable                                              | `PaymentPanel.tsx` 463–496                                           | Click amount → input, `Enter` commits, `Esc` cancels.                                                                                                                                                                             |
| C12 | 🟢  | XS     | Move FX-rate badge out of payment panel header                                            | `PaymentPanel.tsx` 264–277, `POSTerminal.tsx` 197–209                | Move to the cashier topbar (next to "Cerrar caja"); free vertical space in the payment panel. Bump font from `text-[10px]` to `text-xs`.                                                                                          |
| C13 | 🟢  | XS     | Remove unused `X` import                                                                  | `OpenSessionModal.tsx` 4                                             | `import { DollarSign } from "lucide-react";`                                                                                                                                                                                      |
| C14 | 🟡  | M      | Standardise on HeroUI `Modal` for `OpenSessionModal`, `CloseSessionModal`, `ReceiptModal` | `OpenSessionModal.tsx`, `CloseSessionModal.tsx`, `ReceiptModal.tsx`  | Three custom div-backdrop modals migrate to HeroUI `Modal` to match `ConfirmDialog`/`ProductForm`/`StockAdjustModal`. Free focus trap, scroll lock, ESC-to-close.                                                                 |

---

## Stage 3 — Catálogo / Inventario

| ID  | Sev | Effort            | Title                                                         | File(s)                                                 | Acceptance                                                                                                                                                 |
| --- | --- | ----------------- | ------------------------------------------------------------- | ------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------- |
| K1  | 🟡  | XS                | Fix DataTable zebra/hover contrast inversion                  | `DataTable.tsx` 173–181                                 | Drop zebra striping, keep only hover (`hover:bg-[var(--bg-card-elevated)]`). Or change zebra to `bg-white`/`bg-[#fbfaf6]`.                                 |
| K2  | 🟡  | XS                | Symmetrise pagination buttons                                 | `DataTable.tsx` 208–236                                 | Both `Anterior` and `Siguiente` use the same className. Extract to a local `PaginationButton` mini-component.                                              |
| K3  | 🟢  | XS                | Shrink action column width                                    | `ProductList.tsx` 211                                   | `w-32` → `w-24`.                                                                                                                                           |
| K4  | 🟡  | XS                | Add `out_of_stock` to StatusBadge map                         | `StatusBadge.tsx` 10–60                                 | `STATUS_MAP.out_of_stock = "error"`; `LABELS.out_of_stock = "Sin stock"`.                                                                                  |
| K5  | 🟡  | (covered by DS-3) | Use shared `<Tabs>` in CatalogManager + SettingsManager       | `CatalogManager.tsx` 24–52, `SettingsManager.tsx` 38–54 | See DS-3.                                                                                                                                                  |
| K6  | 🟢  | S                 | Add `density` prop to DataTable                               | `DataTable.tsx`                                         | `density: "compact" \| "comfortable"` (default `comfortable`); compact ⇒ `py-2`. Apply `compact` to product/customer/supplier tables.                      |
| K7  | 🟢  | M                 | Add bulk-select column to DataTable + sticky bulk-actions bar | `DataTable.tsx`, `ProductList.tsx`                      | Optional `selectable: boolean` prop; controlled `selectedKeys` set; emits `onSelectionChange`. Sticky "N seleccionados — Acciones ▾" bar in `ProductList`. |
| K8  | 🔴  | (covered by DS-1) | One shared `<Input>` component                                | (5 components)                                          | See DS-1.                                                                                                                                                  |
| K9  | 🟢  | XS                | Live-validate negative-stock-result in StockAdjustModal       | `StockAdjustModal.tsx` 96–119                           | "El stock no puede quedar negativo" fires as `deltaError` while typing, not only on submit.                                                                |
| K10 | 🟢  | XS                | Improve narrow-width quick-add form layout                    | `CategoryList.tsx` 274–316                              | Add `sm:flex-row flex-col` to the form's wrap container.                                                                                                   |
| K11 | 🟢  | XS                | Localise hardcoded "Estado" header                            | `ProductList.tsx` 169, `lib/i18n.ts`                    | New key `t.products.status: "Estado"`; reference it.                                                                                                       |

---

## Stage 4 — Configuración / Setup

| ID  | Sev | Effort | Title                                                   | File(s)                                                                             | Acceptance                                                                                                                                                                                 |
| --- | --- | ------ | ------------------------------------------------------- | ----------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| L2  | 🔴  | XS     | Fix the "Nombre del negocio del negocio" tab string bug | `SettingsManager.tsx` 22–24, `lib/i18n.ts`                                          | Add `t.settings.tab_business: "Negocio"`. Replace the `replace(...) + " del negocio"` line with the new key.                                                                               |
| L3  | 🔴  | S      | De-duplicate theme + branding controls                  | `BusinessSettingsForm.tsx` 218–240, `AppearanceSettings.tsx`, `SettingsManager.tsx` | Move `theme`, `primary_color`, `logo_url` to **Apariencia**. `BusinessSettingsForm` retains only `business_name`, `business_type`, `support_whatsapp`. No setting writeable from two tabs. |
| L8  | 🟢  | XS     | Show step labels on mobile in setup wizard              | `(auth)/setup/page.tsx` 127–134                                                     | Outside the step row, add `<span class="sm:hidden mt-2 text-xs text-[var(--text-muted)]">{currentStepLabel}</span>` so mobile users see the active step name.                              |
| L9  | 🟢  | XS     | Responsive grid for type-of-commerce picker             | `(auth)/setup/page.tsx` 219                                                         | `grid-cols-2 sm:grid-cols-3`.                                                                                                                                                              |
| L10 | 🟢  | M      | Persist setup-wizard Step 1 across reload               | `(auth)/setup/page.tsx` 198–207, backend `setup` endpoint                           | Either auto-save on Step 1 → 2 transition (`PATCH /setup/draft`) or `localStorage.setItem("setup-draft-step1", JSON.stringify(values))`. Restore on mount.                                 |
| L11 | 🟢  | XS     | Standardise buttons in setup wizard                     | `(auth)/setup/page.tsx` 198–264                                                     | Replace HeroUI `<Button>` with the new `<Button>` primitive (DS-2). The type-grid pills stay as-is.                                                                                        |
| L12 | 🟢  | XS     | Add format validation to WhatsApp field                 | `(auth)/setup/page.tsx` 30–35, `BusinessSettingsForm.tsx` 30–37                     | Zod refine: `z.string().regex(/^\+?\d[\d\s]{8,14}$/)`. Use `react-imask` or a simple `formatPhone` helper for the input mask.                                                              |

---

## Stage 5 — Polish

| ID  | Sev | Effort | Title                                                                        | File(s)                                                                                               | Acceptance                                                                                                                                                                                  |
| --- | --- | ------ | ---------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| P-1 | 🟢  | XS     | Drop legacy i18n nav keys                                                    | `lib/i18n.ts` 5–9, all callers                                                                        | Remove `nav.sales` / `nav.products` / `nav.giftCards` / `nav.cashier`; migrate any remaining callers to the standard keys.                                                                  |
| P-2 | 🟢  | XS     | Document `--accent-subtle` alpha intent                                      | `globals.css` 30–32, 86–87                                                                            | Add a comment explaining why dark uses `0.15` and light uses `0.10` — or align them.                                                                                                        |
| P-3 | 🟡  | M      | Document the design system in `docs/design/`                                 | new `docs/design/SYSTEM.md`                                                                           | Component catalogue (props, variants, do/don't). Token reference. Modal pattern. Form pattern. Tab pattern.                                                                                 |
| P-4 | 🟡  | M      | Add visual regression tests (Storybook + Chromatic, or Playwright snapshots) | new `frontend/tests/visual/`                                                                          | At minimum, snapshot the cashier (empty / cart-with-items / paid-with-change), the catalog table, the login page, the setup wizard step 1/2/3. Catches future regressions on token changes. |
| P-5 | 🟢  | S      | Reconcile `mockups/` with current source or archive them                     | `mockups/sales.html`, `mockups/catalog.html`, `mockups/wizard-branding.html`, `mockups/settings.html` | Either update mockups to match source, or move them to `mockups/archive/` with a README noting they're historical.                                                                          |

---

## Quick stats

- **Total tasks:** 45
- **Critical (🔴):** 9
- **Moderate (🟡):** 15
- **Minor (🟢):** 21
- **Quickest wins (XS effort, high or medium severity):** DS-5, DS-7, DS-8, AUTH-3, C1, C5, C7, C13, K1, K2, K4, L2, L8, L9 — about a half-day of work that resolves most of the immediately-visible inconsistencies.
- **Highest-leverage tasks:** DS-1, DS-2, DS-3 — building the three missing primitives unblocks ~12 other tasks and ends the copy-paste cycle.

— end of task list —
